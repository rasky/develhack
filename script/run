#!/bin/bash

set -euo pipefail
IFS=$'\n\t'
cd "$(cd "$(dirname "${0}")"; pwd)/../"

DESMUME_DARWIN_APP="${HOME}/Applications/DeSmuME.app"

if [[ "$(uname -s)" == "Darwin" ]]; then
    exec "${DESMUME_DARWIN_APP}/Contents/MacOS/DeSmuME" "build/game.nds"
fi
