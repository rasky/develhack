#ifndef _DF_DEBUG_H_
#define _DF_DEBUG_H_ 1

// Prints formatted output to the iDeaS debug console
// Returns false when text has not been printed, true otherwise.
int debugf(const char* format, ...);

#endif
