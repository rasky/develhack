#!/bin/bash

set -euo pipefail
IFS=$'\n\t'
cd "$(cd "$(dirname "${0}")"; pwd)/../"

sync
sleep 1

docker run -it -v "$(pwd):/home/code" develhack:latest sh -c "\
    test -d build || ( mkdir -p build && cd build && cmake .. ) \
        && cd build \
        && make VERBOSE=1"
